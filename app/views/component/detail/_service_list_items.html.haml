%section.content
  %section.icon-text-block
    %i.fa.fa-handshake-o{ role: "img", aria: { label: "Services marker" } }
    %h4.annotated-text-block= t('labels.services')
    %ul
      - location.services.each_with_index do |service, i|
        %li
          %i.fa.fa-angle-right{role: 'img', aria: { label: "Item" } }
          %span.annotated-text-block.service-item{ id: "service-item-#{i}", role: 'button' }
            #{service.name}
